// EXACT spec example - template strings with when loop
// Combines template strings AND when loop

int8:c = 0;
int8:i = 3;

when(c <= i) {
    string:msg = `iteration: &{c}`;
    c++;
} then {
    string:success = `when loop ran &{c} times successfully`;
} end {
}