// Test when/then/end loop (Spec Section 8.2)
// Spec syntax: when(condition) { body } then { success } end { failure }

int8:c = 0;
int8:i = 5;

when(c <= i) {
   c++;
} then {
   // Success block - loop completed
} end {
   // End block - loop didn't run or broke early
}
