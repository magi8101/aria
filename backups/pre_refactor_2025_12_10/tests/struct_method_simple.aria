{
// Test struct method - simple version without parameters
const Point = struct {
    x: flt32,
    y: flt32,
    
    // Method as lambda with no parameters (just to test parsing)
    func:getX = () => {
        pass(3.0);
    };
};

func:main = int32() {
    Point:p1 = Point{ x: 3.0, y: 4.0 };
    pass(0);
};
}
