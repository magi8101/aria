// Test lambda parsing in variable assignment WITHOUT immediate call
print("Testing lambda in variable...");

// This will likely fail at codegen since we can't store functions in int8
// But it should at least parse
// Actually this won't parse - type mismatch!

print("Test complete!");
