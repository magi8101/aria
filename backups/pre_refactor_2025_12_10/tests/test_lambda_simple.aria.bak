// Test lambda WITHOUT immediate invocation first
// Just parse and store it

print("Testing lambda storage...");

// This should NOT crash - just store a lambda without calling it
// But wait - we can't store lambdas in variables yet without a func type

print("Test skipped - need function type support first");
