// Minimal closure test - no local vars to avoid parser issues
int8:globalValue = 5;

func:useGlobal = int8(int8:x) {
    return x + globalValue;  // Captures globalValue
};

func:main = int8() {
    return useGlobal(10);  // Should be 10 + 5 = 15
};
