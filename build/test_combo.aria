func:pow_int32 = *int32(int32:base, int32:exponent) {
    if (exponent < 0) {
        fail(1);
    }
    if (exponent == 0) {
        return 1;
    }
    int32:result_val = 1;
    int32:i = 0;
    while (i < exponent) {
        result_val = result_val * base;
        i = i + 1;
    }
    return result_val;
};

func:main = *int8() {
    int32:r = pow_int32(2, 3) ? 0;
    return 0;
};
