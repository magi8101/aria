// test_func_ptr_call.aria
// Test calling through a function pointer (without JIT for now)

// Define a simple function
func:add = int64(int64:a, int64:b) {
    *a + b;
};

func:main = int8() {
    // Call function directly
    int64:result1 = add(10, 20);
    print("Direct call: 10 + 20 = 30 (expected)");
    
    // TODO: Test function pointer call
    // This requires function pointer type syntax which may not be implemented yet
    // Something like:
    // func(int64, int64):int64:add_ptr = add;
    // int64:result2 = add_ptr(10, 20);
    
    *0;
};
