%macro GEN_POW 1
func:pow_%1 = *%1(%1:base, %1:exp) {
    %1:val = base;
    while (val > 0) {
        val = val - 1;
    }
    return val;
};
%endmacro

GEN_POW(int32)

func:main = *int8() {
    int32:p = pow_int32(5, 3) ? 0;
    return 0;
};
