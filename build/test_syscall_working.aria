// Test syscalls with print (which works)
func:main = int8() {
    print("Syscall test starting");
    
    // Test writing to stdout (fd=1) directly
    uint8:msg[32];
    msg[0] = 72;   // 'H'
    msg[1] = 101;  // 'e'
    msg[2] = 108;  // 'l'
    msg[3] = 108;  // 'l'
    msg[4] = 111;  // 'o'
    msg[5] = 10;   // '\n'
    
    int64:written = intrinsic_write(1, msg, 6);
    
    print("Done!");
    pass(0);
};
