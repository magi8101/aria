// Array test without typed variables (for now)
func:sum_array = *int64() {
    // For now, test array literal in expression
    // We'll add proper array variable support next
    return 0;
};

func:main = *int8() {
    return 0;
};
