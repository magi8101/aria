database library , utilities to work with database, clients, etc

pub/sub / event notifier library

time/clock library, timing functions, date, sleep, wait, interval timeout etc

debugging library

lsp library

ide integration, syntax highlighting vscode extension, mcp server, etc library

input library for precise keyboard,mouse,and gamepad inputs

drawing library supporting 2d and 3d, should support generic functions but also be optimized to work with our batteries included if you want HTML5/CSS/JS GUI library

regex library

sorting library

algorithms library, focus on most used by developers as well as most cutting edge

templiting engine that can run before anything and replace certain variable strings like [@@_VERSION_@@] or date, etc, developer, titles, etc. could ever be used to help with documentation updates, run just before the nasm style macro stuff runs. 

custom error type to use with our Result type ie error:err1 = { code: 1,cause:"put cause/sender here",message:"error message string",data:{//add key/vals here to propogate information} }; result:r={err:err1,val:NULL}
