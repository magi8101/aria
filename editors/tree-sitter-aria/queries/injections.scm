; Injection queries for Aria
; Used for syntax highlighting embedded languages

; None for now - could add later for things like:
; - Inline assembly
; - Documentation strings with markdown
; - SQL queries in string literals
