// Test file inclusion with command-line defines
%include "include_test.aria"

%ifdef PRODUCTION
LOG("Running in production mode")
%else
LOG("Running in development mode")
%endif

let buffer: [u8; BUFFER_SIZE];
let max_conn: int = MAX_CONNECTIONS;
