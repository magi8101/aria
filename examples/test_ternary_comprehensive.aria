// Comprehensive test of the is ternary operator
// Tests: basic usage, nested ternary, comparison operators, type consistency

func:main = int8(){
    // Basic ternary: is condition : true_val : false_val
    int8:x = 11;
    int8:y = is x == 11 : 100 : 200;
    
    // Ternary with different condition
    int8:a = 5;
    int8:b = is a > 10 : 1 : 0;
    
    // Nested ternary
    int8:c = is x == 11 : (is a > 3 : 50 : 25) : 0;
    
    pass(0);
};
