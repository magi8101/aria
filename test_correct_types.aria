// Test program with correct types
// Should pass type checking without errors

// Valid integer declarations
int64:x = 42;
int64:y = 100;

// Valid arithmetic
int64:sum = x + y;
int64:diff = x - 10;
int64:prod = x * 2;

// Valid boolean declarations
bool:flag1 = true;
bool:flag2 = false;

// Valid comparisons (produce boolean)
bool:cmp1 = x > y;
bool:cmp2 = x == 42;
bool:cmp3 = sum != diff;

// Valid logical operations
bool:and_result = flag1 && flag2;
bool:or_result = flag1 || flag2;
