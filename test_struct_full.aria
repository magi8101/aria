// Complete struct test - Point with constructor and member access
const Point = struct {
    x: int64,
    y: int64,
};

func:main = *int8() {
    // Create a Point using struct constructor
    Point:p = Point{x: 10, y: 20};
    
    // Access members
    int64:px = p.x;
    int64:py = p.y;
    
    // Should print: px=10, py=20
    // For now, just return success
    return 0;
};
