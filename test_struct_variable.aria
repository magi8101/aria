// Test struct declaration with usage
const Point = struct {
    x: int64,
    y: int64,
};

func:main = *int8() {
    // Declare a Point variable (will test allocation once constructor syntax works)
    Point:p;  // This should allocate space for a Point struct
    
    return 0;
};
