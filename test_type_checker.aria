// Test program for type checker
// Contains intentional type errors to verify type checking works

// Test 1: Valid variable declaration and assignment
int64:x = 42;

// Test 2: Valid arithmetic
int64:sum = x + 10;

// Test 3: Type mismatch in assignment (should error: bool assigned to int)
int64:y = true;

// Test 4: Arithmetic with wrong types (should error: can't add int and bool)
int64:z = x + false;

// Test 5: Valid comparison
bool:cmp = x > 5;

// Test 6: Type mismatch in comparison (should error: comparing int with bool)
bool:bad_cmp = x == true;
