// Test simple break in till loop
func:test = int32(int32:x) {
    pass(x);
};

func:main = int32() {
    till(100, 1) {
        test($);
        break;  // Break immediately after first iteration
    }
    pass(0);
};
