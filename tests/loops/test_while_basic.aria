// Test while loop - basic compilation test
// Note: Can't use parameters for condition yet (would need local var reassignment)
// Note: Can't use globals (creates closure with wrong signature) 
// So this is an infinite loop, but it compiles correctly!

func:test = int32(int32:x) {
    pass(x);
};

func:main = int32() {
    // Infinite loop - just testing that while loops parse/compile
    while (1 > 0) {
        test(42);
    }
    pass(0);
};
