{
// Test actual member access on returned objects
// This tests the full flow: function call -> object return -> member access

print("Testing full member access flow...");

// Simplified: just return the value directly (not object literal)
// TODO: Add proper Result type support for object literal returns
func:makeResult = *int8() {
    return 42;
};

// TODO: Once we can call functions and store results, test member access
// For now, function parsing and value return works!

print("Test complete!");
}
