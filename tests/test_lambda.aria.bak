// Test lambda expressions - JavaScript module-style!
// Lambda syntax: returnType(params) { body }
// Immediately invoked: returnType(params){body}(args)
//
// ARIA MODULE PHILOSOPHY:
// - All code runs in module init (like JS modules)
// - Types are ALWAYS explicit (no inference ambiguity)
// - Works predictably everywhere (no "sometimes this, sometimes that")
// - Simple, deterministic, ASD/OCD-friendly design

print("Testing lambdas at module scope...");

// Immediately invoked lambda with explicit return type
// All variables require explicit types - no exceptions!
// Note: Variable type must match lambda return type
int8:output = int8(int8:a, int8:b) {
    return a + b;
}(10, 20);

print("Lambda executed! Result = 10 + 20 = 30");

print("Lambda test complete!");

// Optional: define a main function that will be called after module init
func:main = int8() {
    print("Main function called after module initialization!");
    return 0;
}
