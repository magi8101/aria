{
// Test with explicit type matching lambda return
print("Creating GC variable...");

// ARIA RULE: All variables need explicit types
// Variable type MUST match the expression type
// int8 lambda returns int8, so variable must be int8
int8:output = int8(int8:a, int8:b){
    return a + b;
}(10, 20);

print("GC variable with lambda output created!");
}
