// Test member access on object literals (Result type simulation)
// Testing r.err and r.val access
{
print("Testing member access...");

// For now, we'll skip the Result type declaration since 'output' type isn't in lexer
// and just test that object literals with member access work

func:divide = int8(int8:a, int8:b) {
    return {
        err: NULL,
        val: a + b
    };
}

print("Member access test complete!");
}
