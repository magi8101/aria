{
// Test Result type with new syntax
// func:name = returnType(params) { return { err: NULL, val: value }; }

func:add = int8(int8:a, int8:b) {
    int8:output = a + b;
    return {
        err: NULL,
        val: output
    };
};

print("Testing Result type...");
// output:r = add(10, 5);

// Access Result fields
// int8:value = is r.err == NULL : r.val : -1;

print("Result test complete!");
}
