func:test_interp = int8() {
    int8:value = 42;
    int8:sum = 0;
    
    till(5, 1) {
        sum = sum + $;
        // Test string interpolation with $ and local var
        print(`Iteration &{$}: sum = &{sum}, value = &{value}`);
    }
    
    return sum;
};
